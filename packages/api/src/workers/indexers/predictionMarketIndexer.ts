/**
 * Re-export barrel — the implementation has moved to ./predictionMarket/
 * This file exists so that all existing import paths continue to work.
 */
export { default } from './predictionMarket';
