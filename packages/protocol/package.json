{
  "name": "@foil/protocol",
  "license": "UNLICENSED",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "cannon build --keep-alive --port 8545 --no-cors --write-deployments ./deployments/13370",
    "test": "forge build && cannon build --write-deployments ./deployments && cannon test",
    "test2": "forge build && cannon build cannonfile.test.toml --write-deployments ./deployments && cannon test cannonfile.test.toml ",
    "deploy:sepolia": "cannon build cannonfile.sepolia.toml --chain-id 11155111 --write-deployments ./deployments/11155111",
    "simulate-deploy:sepolia": "cannon build cannonfile.sepolia.toml --chain-id 11155111 --dry-run"
  },
  "devDependencies": {
    "@types/chai": "^4.3.16",
    "@types/mocha": "^10.0.7",
    "@types/node": "^20.14.13",
    "chai": "^4.5.0",
    "forge-std": "github:foundry-rs/forge-std#master",
    "mocha": "^10.7.0",
    "mocha-junit-reporter": "^2.2.1",
    "solidity-coverage": "^0.8.12",
    "ts-node": "^10.9.2",
    "typechain": "^8.3.2",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "@uma/core": "^2.59.1",
    "@uniswap/v3-core": "^1.0.1",
    "@uniswap/v3-periphery": "^1.4.4",
    "@usecannon/cli": "^2.16.0"
  }
}
