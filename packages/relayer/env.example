# Relayer WebSocket Service - Environment Variables
# 
# Note: This service loads environment variables from the repo root .env file
# Copy this example to the repo root as .env and configure as needed

# ============================================================================
# REQUIRED - Server Configuration
# ============================================================================

# Environment mode: 'development', 'production', or 'test'
NODE_ENV=development

# Server port (default: 3002)
# Note: In Render/production, PORT is automatically set - don't override it
PORT=3002

# ============================================================================
# OPTIONAL - Server Configuration
# ============================================================================

# Enable the auction WebSocket server (default: true)
ENABLE_AUCTION_WS=true

# Sentry DSN for error tracking (required in production)
# Get this from your Sentry project settings
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Rate limiting configuration
# Window duration in milliseconds (default: 10000 = 10 seconds)
RATE_LIMIT_WINDOW_MS=10000
# Maximum number of messages allowed per window (default: 100)
RATE_LIMIT_MAX_MESSAGES=100

# WebSocket connection configuration
# Idle timeout in milliseconds (default: 300000 = 5 minutes)
# Connections idle longer than this will be closed
# Note: Server responds to client pings automatically (no server-initiated ping)
WS_IDLE_TIMEOUT_MS=300000
# Maximum concurrent connections (default: 1000)
WS_MAX_CONNECTIONS=1000
# Allowed origins (comma-separated, empty = allow all)
# Example: WS_ALLOWED_ORIGINS=https://app.sapience.xyz,https://relayer.sapience.xyz
WS_ALLOWED_ORIGINS=

# ============================================================================
# OPTIONAL - Blockchain RPC Configuration
# ============================================================================

# Infura API key for blockchain RPC calls
# Used for: Mainnet, Sepolia, Base, Arbitrum WebSocket connections
# Only needed when handling vault_quote.publish/submit messages
# If not provided, falls back to public HTTP RPC endpoints
INFURA_API_KEY=your_infura_api_key_here

# Custom RPC URL for Converge chain (chainId 432)
RPC_URL=https://your-converge-rpc-url.com

# Custom RPC URL for EtherealChain (chainId 5064014)
# Default: https://rpc.ethereal.trade
CHAIN_5064014_RPC_URL=https://rpc.ethereal.trade


